import FilteredButtonsList from "./FilteredButtonsList";
import VideoContainer from "./VideoContainer";

const MainContainer = () => {
  return (
    <div className="w-11/12">
      <FilteredButtonsList />
      <VideoContainer />
    </div>
  );
};

export default MainContainer;
